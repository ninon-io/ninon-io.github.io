# Build output
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Ruby
.bundle/
vendor/

# Node (not used, guard only)
node_modules/

# Editors
*~
*.sw[p_]
*.sublime-project
*.sublime-workspace

# macOS
.DS_Store

# NOTE: Gemfile.lock is deliberately committed. It is what makes the local build
# and the GitHub Actions build identical.
