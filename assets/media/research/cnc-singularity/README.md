# Unpublished — pending identification

`figure-9-sweet-spot.png` was added to the repository on 23 Aug 2026. The folder
name suggests it belongs to *Singularity and Creation in the Age of Artificial
Intelligence* (Devis & Suzuki, 2026), listed on the CV as published by "CNC Lab".

**CNC Lab has not been identified**, so that publication is deliberately absent
from `_data/publications.yml` and this image is referenced by no page. The asset
is kept so it is not lost.

To publish: identify the venue, add the entry to `_data/publications.yml`, then
place the figure with `{% include site/img.html %}`.
