# Archive

Material that is in the repository but referenced by no page. Kept rather than
deleted because it may still be wanted. Nothing here is published in a page today.

## images/

Alternative header crops and unused photographs. Renamed for consistency only;
content is unchanged from the original `images/` folder.

## audio/

Eleven audio files that no page has ever linked to.

**These need your triage — I could not determine what they are.** Six are named
after what look like surnames (`abrassart`, `angulo`, `chable`, `le`, `verrier`)
and five after track titles (`cosmic-waves`, `experiences`, `love-story`,
`purehousedata`, `sing-with-me`, `une-minute`). They are plausibly ATIAM student
submissions, but they could equally be your own material — so they were left
unclassified rather than filed under `teaching/`.

Note: `purehousedata.mp3` was byte-identical to a file named `uzan.mp3`; the
duplicate was removed and the descriptive name kept. If `uzan` was the correct
name, rename it back.
